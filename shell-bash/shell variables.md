variables are chunks of memory to which we can assign values,read and manipulate its
contents
x=1
shell variables
x  = shell variable name
1 =  value

using shell variables 
echo $x

shell variable is a named caontainer used to store data ine memory while a shell script is running

when you set variable in terminal you can use them in scripts

to avoid a lot of troubles we can use {} to separate variable from the text