merges duplicate lines into one 
uniq option input(option)
uniq dont merges lines when its not close to each other soo better use sort before
 options -c count duplicates
	  -d prints only options that are repeated
	  -D prints repeated lines and discards non dupliate lines
	  -u print only non duplicate
	  -i ignore case opitons
	  -f skiping fields
	  -s skip characters
	  -w consider characters