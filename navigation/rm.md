removing files and direcotries
rm file.txt
	removes file
rm -i
	use the flag to ask to remove file every time
rm -v 
	shows what is being deleted
rmdir directory
	-deletes directory
rm -r directory
	removes directory with files inside
rm (star).txt 
	romoves every file with txt
rm -f 
	force removing without prompting 


