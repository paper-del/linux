shows list of directories and files in directory in which  you are
ls -l list long () shows detailed list
la -a shows hiden files
ls -h shows files size in human readable version
ls -R shows content thick directory and content in directories inside 
