The PATH variable contains a set of directories where executable programs such as ping, date, vi, docker and others are stored on Linux or Unix-like systems
echo $PATH

export PATH=
	ads new directory to a path 
**export PATH="$PATH:/opt/myapp/bin"**
	appends directory to a path
	