move
mv file directory 
	moves file to directory
mv file1 file2 file3 directory
	moves 3 files to directory
mv file directory/new_file_name 
	changing file name

mv source_directory target_directory
	move source directory to target directory
mv source_directory path_to_non_existing_directory
	reanming directory

mv -n 
	prevents overwriting files
mv -i 
	ask you if this file can be overwriten
mv -b 
	this command overwrite a file but makes automate backup
mv -u
	overwrites if the target is old or doesnt exist
mv -s -b 
	sets where but backup files
mv -f 
	forcefully moves the files 
